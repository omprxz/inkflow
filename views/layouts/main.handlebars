<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} | InkFlow</title>
    <link rel="stylesheet" href="/css/style.css">
    <!-- Simple CSS reset and basic styling -->
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f9f9f9;
        }
        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 20px;
        }
        header {
            background-color: #2c3e50;
            padding: 1rem 0;
            margin-bottom: 2rem;
        }
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo a {
            color: #fff;
            font-size: 1.8rem;
            text-decoration: none;
            font-weight: bold;
        }
        .nav-links {
            display: flex;
            list-style: none;
        }
        .nav-links li {
            margin-left: 1.5rem;
        }
        .nav-links a {
            color: #fff;
            text-decoration: none;
        }
        .btn {
            display: inline-block;
            background: #3498db;
            color: #fff;
            padding: 0.6rem 1.2rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
        }
        .btn:hover {
            background: #2980b9;
        }
        footer {
            background: #2c3e50;
            color: #fff;
            text-align: center;
            padding: 1rem;
            margin-top: 3rem;
        }
        .card {
            background: #fff;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            margin-bottom: 1.5rem;
            padding: 1.5rem;
        }
        .form-group {
            margin-bottom: 1rem;
        }
        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: bold;
        }
        .form-control {
            width: 100%;
            padding: 0.5rem;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        .form-control:focus {
            outline: none;
            border-color: #3498db;
        }
        .alert {
            padding: 0.75rem 1rem;
            margin-bottom: 1rem;
            border-radius: 4px;
            color: #721c24;
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <nav>
                <div class="logo">
                    <a href="/">InkFlow</a>
                </div>
                <ul class="nav-links">
                    <li><a href="/blogs">Blogs</a></li>
                    <li><a href="/blogs/trending">Trending</a></li>
                    {{#if isAuthenticated}}
                        <li><a href="/blogs/dashboard">My Blogs</a></li>
                        <li><a href="/blogs/create">Write</a></li>
                        <li><a href="/auth/logout">Logout</a></li>
                    {{else}}
                        <li><a href="/auth/login">Login</a></li>
                    {{/if}}
                </ul>
            </nav>
        </div>
    </header>

    <main class="container">
        {{#if error}}
            <div class="alert">{{error}}</div>
        {{/if}}
        
        {{{body}}}
    </main>

    <footer>
        <div class="container">
            <p>&copy; {{getCurrentYear}} InkFlow. All rights reserved.</p>
        </div>
    </footer>

    <script>
        // Simple helper function to get current year
        function getCurrentYear() {
            return new Date().getFullYear();
        }
    </script>
</body>
</html>